import { observer } from "mobx-react-lite";
import useStore from "../hooks/useStore";

export const saveUserSettings = observer((user, settings) => {
    const store = useStore()
    
});
export const getUserSettings =observer( (user) => {});
